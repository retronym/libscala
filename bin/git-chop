#!/usr/bin/env bash
#
# Deletes the currently checked out branch,
# after switching to master.

CURRENT=$(git symbolic-ref HEAD)
git checkout -f master || exit 1
git branch -D ${CURRENT##refs/heads/}
