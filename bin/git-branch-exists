#!/usr/bin/env bash
#

if [[ $# -eq 0 ]]; then
  cat <<EOM
Usage: $0 <branch> [branch ...]
Exits zero if all given branches exist in git repo, non-zero otherwise.
EOM
  exit 127
fi

for arg; do
  git show-ref --verify --quiet refs/heads/$arg || exit 1
done
